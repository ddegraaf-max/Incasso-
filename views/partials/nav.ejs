<nav class="nav">
  <%- include('logo', { tagline: page !== 'app' && page !== 'admin' }) %>
  <span class="nav-right">
    <% if (page === 'app' || page === 'admin') { %>
      <%= user ? user.company : '' %><% if (user && user.totpConfirmed) { %> · 2FA<% } %>
      <% if (user && user.role === 'admin') { %><a href="/admin">Admin</a><% } %>
      <a href="/app/sprawy">Panel</a>
      <a href="/logout">Wyloguj</a>
    <% } else if (page === 'auth') { %>
      <a href="/">Strona główna</a>
      <a href="/kalkulator">Kalkulator odsetek</a>
    <% } else { %>
      <a href="/#wycena">Sprzedaj fakturę</a>
      <a href="/windykacja">Windykacja</a>
      <a href="/kalkulator">Kalkulator odsetek</a>
      <a href="/login">Panel klienta</a>
    <% } %>
  </span>
</nav>
<% if (page === 'app') { %>
<div class="tabbar">
  <a href="/app/sprawy" class="<%= tab === 'sprawy' ? 'active' : '' %>">Sprawy</a>
  <a href="/app/nowa" class="<%= tab === 'nowa' ? 'active' : '' %>">Nowa sprawa</a>
  <a href="/app/agent" class="<%= tab === 'agent' ? 'active' : '' %>">Agent AI</a>
  <a href="/app/wykup" class="<%= tab === 'wykup' ? 'active' : '' %>">Wykup</a>
</div>
<% } %>
